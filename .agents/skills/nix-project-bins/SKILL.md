---
name: nix-bins
description: Use when trying to run a binary in the bash tool
---

# Nix project binaries

- Some binaries are not in path, in that situatuion inspect nix flake for devshell.
- The devshell should be modified if a new binary is needed
- use `nix devlop -c` for access to devshell binaries

